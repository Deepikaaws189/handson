- name: vars
  hosts: local
  connection: local
  vars_file:
  - details.yaml
  tasks: #vars
  - name: vars
    ansible.builtin.debug:
      msg: "my name is {{ call }} and im from {{ place }}"
  - name: vars
    vars_file:
    - localvars.yaml
    ansible.builtin.debug:
      msg: "my name is {{ call }} and im from {{ place }}"